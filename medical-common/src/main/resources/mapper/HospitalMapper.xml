<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.incaier.integration.platform.mapper.HospitalMapper">
    <select id="getAgencies" resultType="com.incaier.integration.platform.response.HospitalVo">
        select
            id,
            hospital_id,
            hospital_name
        from
            hospital
    </select>
</mapper>